<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="middleTierRelated.dao.UserDao">

	<sql id="userColumns">
		id, login_code, user_name
	</sql>

	<select id="selectAll" resultType="middleTierRelated.vo.UserVO">
		select
		<include refid="userColumns" />
		from sys_user
	</select>

	<select id="selectById" resultType="middleTierRelated.vo.UserVO">
		select
		<include refid="userColumns" />
		from sys_user where login_code=#{id}
	</select>

	<select id="selectByLoginCode"
		resultType="middleTierRelated.vo.UserVO">
		select
		<include refid="userColumns" />
		from sys_user where login_code=#{loginCode}
	</select>

	<insert id="insert" parameterType="entity.UserPO">
		insert into sys_user (id,
		login_code, user_name, password,
		password_salt, email, status)
		values
		(#{id,jdbcType=BIGINT},
		#{loginCode,jdbcType=VARCHAR},
		#{userName,jdbcType=VARCHAR},
		#{password,jdbcType=VARCHAR},
		#{passwordSalt,jdbcType=VARCHAR},
		#{email,jdbcType=VARCHAR},
		#{status,jdbcType=CHAR})
	</insert>

	<update id="update">
		update sys_user
		<set>
			<if test="user.id != null">
				id = #{user.id,jdbcType=BIGINT},
			</if>
			<if test="user.loginCode != null">
				login_code = #{user.loginCode,jdbcType=VARCHAR},
			</if>
			<if test="user.userName != null">
				user_name = #{user.userName,jdbcType=VARCHAR},
			</if>
			<if test="user.password != null">
				password = #{user.password,jdbcType=VARCHAR},
			</if>
			<if test="user.passwordSalt != null">
				password_salt = #{user.passwordSalt,jdbcType=VARCHAR},
			</if>
			<if test="user.email != null">
				email = #{user.email,jdbcType=VARCHAR},
			</if>
			<if test="user.status != null">
				status = #{user.status,jdbcType=CHAR},
			</if>
		</set>
	</update>

	<delete id="deleteById">
		delete from sys_user where id = #{id}
	</delete>
</mapper>